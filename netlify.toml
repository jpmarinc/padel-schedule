[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Supabase anon key es pública por diseño — la seguridad viene de RLS en la BD
  SECRETS_SCAN_OMIT_KEYS = "VITE_SUPABASE_ANON_KEY,VITE_SUPABASE_URL"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
